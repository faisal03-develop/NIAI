# ============================================================
# requirements.txt
# ============================================================
# Lists every Python package the project needs.
# Install all with: pip install -r requirements.txt
#
# WHY PIN VERSIONS?
#   Without pinned versions, pip installs the LATEST version today,
#   but in 6 months it may install a different version with breaking
#   changes. Pinning ensures the same environment everywhere.
# ============================================================

# ── Core data & ML ───────────────────────────────────────────
numpy>=1.26.4
pandas>=2.2.2
scikit-learn>=1.5.0
flask>=3.0.3
gunicorn>=22.0.0
pytest>=8.2.2
pytest-cov>=5.0.0
flake8>=7.0.0
flask-cors>=4.0.0